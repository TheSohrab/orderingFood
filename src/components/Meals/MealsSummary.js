import classes from "./MealsSummary.module.css";

const MealsSummary = () => {
  return (
    <section className={classes.summary}>
      <h2>“Welcome to Sohrab’s first site”</h2>
      <p>created using CSS modules, JavaScript, and React.</p>
    </section>
  );
};

export default MealsSummary;
